# README
## Docker
build with 
```
docker build -t *<{{your preferred name}}>*
```

run with

```
docker run -p 3000:3000 *<{{your preferred name}}>*
```

## Docker-compose
build with
```
docker-compose build
```

run with
```
docker-compose up
```
